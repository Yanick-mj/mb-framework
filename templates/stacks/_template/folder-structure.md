# {Stack Name} — Folder Structure

```
project/
├── src/
│   └── ...
├── tests/
│   └── ...
└── package.json (or equivalent)
```

Describe the standard folder layout for this stack.
